# Lotus Hub environment variables
# Phase 1 requires no secrets. Future phases (Firebase, Cloudflare Workers,
# external storage) will add values here. NEVER commit real secrets.

VITE_APP_NAME=Lotus Hub
